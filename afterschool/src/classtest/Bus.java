package classtest;

public class Bus extends Transport{

	public Bus(Passenger passenger, int money, int number, String startLocation, String endLocation,
			String transLocation) {
		super(passenger, money, number, startLocation, endLocation, transLocation);
	}
	// 속성
	// 승객, 요금, 버스번호, 출발지, 도착지, 경유지
	
	// 기능(동작)
	// 태우다
	
}
